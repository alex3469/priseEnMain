<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link rel="stylesheet" type="text/css" href="{{ asset('./style.css')}}"/>
    </head>
    <body>
        {% block body %}
        <body>
            {#On cree un block nav qui est rappelé dans nos autres pages #}
            {% block nav %}
                <nav>
                    <ul>
                        {# On link grace au path (name) la route #}
                        <li><a href="{{path('yellow')}}">yellow</a></li>
                        <li><a href="{{path('red')}}">red</a></li>
                        <li><a href="{{path('blue')}}">blue</a></li>
                        <li><a href="{{path('green')}}">green</a></li>
                    </ul>
                </nav>
            {% endblock %}
        </body>
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
